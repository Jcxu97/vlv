# 可选：Windows 上为 faster-whisper / ctranslate2 提供 CUDA 12 的 cuBLAS、cuDNN 等 DLL
# 安装后一般无需再装完整 CUDA Toolkit；仍需较新的 NVIDIA 显卡驱动。
# 用法：pip install -r requirements-gpu.txt
nvidia-cublas-cu12>=12.1.3.1
nvidia-cudnn-cu12>=9.1.0.70
